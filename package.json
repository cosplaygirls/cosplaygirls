{
  "name": "cosplaygirls",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.11.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "npm run yaml2json && react-scripts start",
    "build": "npm run yaml2json && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "yaml2json": "yaml2json cosplaygirls.yml > ./public/cosplaygirls.json",
    "postbuild": "react-snap"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "react-snap": "^1.23.0",
    "yamljs": "^0.3.0"
  }
}
